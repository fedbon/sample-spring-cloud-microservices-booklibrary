server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fedbon/config-repo.git
#        bootstrap: true
#        encrypt:
#          enabled: false
#  security:
#    user:
#      name: root
#      password: root